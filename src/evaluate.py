"""
Zar3y — Model Evaluation (Requirement 2)

This module handles:
1. Loading the best model and test dataset
2. Computing overall accuracy + macro F1
3. Per-class precision / recall / F1
4. Generating confusion matrix as PNG
5. Generating training + validation loss/accuracy curves as PNG
6. Saving all metrics to outputs/eval_report.json

Deliverables:
- outputs/confusion_matrix.png
- outputs/training_curves.png
- outputs/eval_report.json
"""

# TODO: Implement evaluation pipeline
#
# Key functions to implement:
#
# def load_test_data():
#     """Load held-out test split."""
#     pass
#
# def compute_metrics(y_true, y_pred, class_names):
#     """Compute accuracy, macro F1, per-class P/R/F1."""
#     pass
#
# def plot_confusion_matrix(y_true, y_pred, class_names, output_path):
#     """Generate and save confusion matrix as PNG."""
#     pass
#
# def plot_training_curves(history, output_path):
#     """Generate and save training/validation curves as PNG."""
#     pass
#
# def save_eval_report(metrics, output_path):
#     """Save all metrics to JSON."""
#     pass
#
# if __name__ == "__main__":
#     # Run full evaluation pipeline
#     pass
