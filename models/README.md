# Models Directory

This directory stores trained model artifacts:

- `best_model.h5` — Best Keras model checkpoint (float32)
- `model_quantized.tflite` — INT8 quantized TFLite model

> These files are excluded from git via `.gitignore` due to size.
> Use Git LFS or upload as release artifacts.
